# Example environment configuration for notes_backend

# App metadata
APP_TITLE=Notes API
APP_DESCRIPTION=A simple FastAPI backend for managing notes (create, list, fetch, update, delete).
APP_VERSION=0.1.0

# Comma-separated list of origins for CORS. Use "*" to allow all origins (default).
# Example: http://localhost:3000,https://myapp.example.com
CORS_ALLOW_ORIGINS=*

# SQLite database path. Use ':memory:' for in-memory DB (non-persistent).
# Default: notes.db (creates a local SQLite file)
NOTES_DB_PATH=notes.db
